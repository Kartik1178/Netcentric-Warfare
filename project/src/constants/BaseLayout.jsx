// BaseLayout.js
export const BASE_SIZE = 300;
export const MAP_WIDTH = 1000; // same as GridCanvas!
export const MAP_HEIGHT = window.innerHeight * 0.9;

export const BASE_START_X = (MAP_WIDTH - BASE_SIZE) / 2;
export const BASE_START_Y = (MAP_HEIGHT - BASE_SIZE) / 2;
