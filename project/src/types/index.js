// Type definitions for reference - these are now just comments in JavaScript

/*
export interface ThreatData {
  id: string;
  name: string;
  category: 'air' | 'land' | 'sea' | 'submarine';
  type: string;
  origin: string;
  specifications: {
    range: string;
    speed: string;
    payload: string;
  };
}

export interface MissileParameters {
  speed: number;
  altitude: number;
  distance: number;
  direction: number;
  threatType: string;
}

export interface LogEntry {
  id: string;
  timestamp: string;
  type: 'detection' | 'analysis' | 'response' | 'intercept' | 'status';
  message: string;
  priority: 'low' | 'medium' | 'high' | 'critical';
}

export interface RadarContact {
  id: string;
  x: number;
  y: number;
  type: 'threat' | 'friendly' | 'unknown';
  status: 'active' | 'intercepted' | 'neutralized';
  speed: number;
  heading: number;
}
*/